# rdc-supervisor
multi agent orchestrator which manages metadata-agent(fetches metadata for mesh and graphql agents for context), mesh-agent (snowflake sql agent), graphql-agent (creates and runs graphql queries)
